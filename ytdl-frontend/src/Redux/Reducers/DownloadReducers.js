const initialState = {

}

export default DownloadReducer(state = initialState, action){
    switch(action.type){
        case:   {
            return{
                ...state, 
                //.....
            }
        }
    }
}